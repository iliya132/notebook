services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: notebook
      POSTGRES_USER: notebook
      POSTGRES_PASSWORD: notebook
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U notebook -d notebook"]
      interval: 3s
      timeout: 3s
      retries: 20
    volumes:
      - notebook-postgres:/var/lib/postgresql/data
volumes:
  notebook-postgres:
